sonar.projectKey=iaas-api-mgmt
sonar.projectName=iaas-api-mgmt

# Code lives at the repo root (not in src/), so point sources at the real dirs
sonar.sources=server.js,lib,public
sonar.exclusions=node_modules/**,.git/**,.env,**/*.min.js
